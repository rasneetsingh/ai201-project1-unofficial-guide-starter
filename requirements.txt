sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5):
# gradio 5+/6+ needs Python 3.10+; this venv is 3.9, so pin to the latest 4.x.
gradio>=4.44,<5
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
